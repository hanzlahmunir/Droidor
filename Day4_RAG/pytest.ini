[pytest]
# pythonpath = . is required or pytest cannot import the `app` package and
# every test fails with ModuleNotFoundError. Day 1's CI failed on exactly this.
pythonpath = .
testpaths = tests
python_files = test_*.py
