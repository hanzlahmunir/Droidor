[pytest]
# Run from the project root so `alembic.ini`, the `app` package, and the
# `alembic` package are all importable/findable.
testpaths = tests
# Show a short summary of failures and print the reason for skips.
addopts = -ra
